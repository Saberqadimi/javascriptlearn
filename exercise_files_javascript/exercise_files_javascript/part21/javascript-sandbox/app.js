const sum = 20;

const otherSum = 25;

function random() {
  return Math.random();
}

const array = [1, 2, 3, 4, 5];

const obj = { a: 1, b: 2, c: 3 };

export { sum,
        otherSum,
        random, 
        array, 
        obj
     };
